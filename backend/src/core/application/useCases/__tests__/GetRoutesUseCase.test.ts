
import { GetRoutesUseCase } from '../GetRoutesUseCase';

describe('GetRoutesUseCase', () => {
  it('should be defined', () => {
    expect(GetRoutesUseCase).toBeDefined();
  });

  it('basic test - always passes', () => {
    expect(1 + 1).toBe(2);
  });
});
